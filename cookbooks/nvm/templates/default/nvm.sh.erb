# Generated by Chef for <%= node['fqdn'] %>
# Local modifications will be overridden

<% if @user_install %>
if [ -f "${HOME}/.nvm/nvm.sh" ]; then
  . "${HOME}/.nvm/nvm.sh"
fi
<% else %>
if [ -f "<%= node['nvm']['directory'] %>/nvm.sh" ]; then
  . "<%= node['nvm']['directory'] %>/nvm.sh"
fi
<% end %>
